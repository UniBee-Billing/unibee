---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: unibee-admin-protal
  namespace: default
spec:
  selector:
    matchLabels:
      app: unibee-admin-protal
  replicas: 1
  template:
    metadata:
      labels:
        app: unibee-admin-protal
    spec:
      restartPolicy: Always
      containers:
        - name: unibee-admin-protal
          image: unibee/admin-portal:stage
          imagePullPolicy: Always
          env:
            - name: UNIBEE_API_URL
              valueFrom:
                configMapKeyRef:
                  name: unibee-config
                  key: UNIBEE_API_URL
          ports:
            - containerPort: 80
              name: http
          
---
apiVersion: v1
kind: Service
metadata:
  name: unibee-admin-protal-svc
  namespace: default
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: unibee-admin-protal